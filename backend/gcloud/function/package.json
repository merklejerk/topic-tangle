{
  "name": "function",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "private": true,
  "author": "Lawrence Forman <me@merklejerk.com>",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "npx @google-cloud/functions-framework --target=tangle",
    "dev": "concurrently -k \"npm:watch\" \"npm run dev:build --prefix ../../mock\" \"nodemon --watch dist/index.js --watch .env.* --exec \\\"npx @google-cloud/functions-framework --target=tangle\\\"\""
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^4.0.0",
    "mock": "file:../../mock"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "concurrently": "^9.2.0",
    "express": "^4.21.2",
    "express5": "npm:express@^5.1.0",
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  }
}
